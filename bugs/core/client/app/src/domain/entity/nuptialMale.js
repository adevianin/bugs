class NuptialMale {

    constructor(id, genome, stats, isLocal) {
        this.id = id;
        this.genome = genome;
        this.stats = stats;
        this.isLocal = isLocal;
    }
}

export {
    NuptialMale
}