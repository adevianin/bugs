import { EntityView } from './entityView';

class FoodAreaView extends EntityView { 

    constructor(entity, entityContainer) {
        super(entity, entityContainer);
    }

    remove() {
        super.remove();
    }
}

export {
    FoodAreaView
}